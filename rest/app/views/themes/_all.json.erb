<% cache(:action => 'index', :action_suffix => 'all_themes') do %>
<%= raw Theme.order("#{Theme.table_name}.theme_name ASC").includes(:children).top.all.as_tree(:checked => params[:checked]).to_json -%>
<% end %>
