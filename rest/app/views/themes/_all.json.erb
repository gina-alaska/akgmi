<% cache(:action => 'index', :action_suffix => 'all_themes') do %>
<%= raw Theme.top.order("#{Theme.table_name}.theme_name ASC").includes(:children).all.as_tree(:checked => params[:checked]).to_json -%>
<% end %>